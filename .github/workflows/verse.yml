name: Verse of the Day to Discord (Test-Safe)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 11 * * *" # 7am Eastern

jobs:
  post-verse:
    runs-on: ubuntu-latest
    steps:
      - name: Post verse to Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{
              "content": "📖 Verse of the Day\n\n\"Your word is a lamp to my feet and a light to my path.\"\n— Psalm 119:105"
            }' \
            "$DISCORD_WEBHOOK"
